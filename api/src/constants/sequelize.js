const PROJECT_ROOT_PATH = "src";
const DB_CONFIG_BASE_PATH = `${PROJECT_ROOT_PATH}/db`;

const PATH_KEYS = ["models-path", "seeders-path", "migrations-path"];

module.exports = { PROJECT_ROOT_PATH, DB_CONFIG_BASE_PATH, PATH_KEYS };
